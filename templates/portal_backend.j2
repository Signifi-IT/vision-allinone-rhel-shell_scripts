backend {{ portal_url | replace('-', '_') | replace('.', '_') }}_backend
    mode http
    server {{ portal_url | replace('-', '_') | replace('.', '_') }}_apache 127.0.0.1:7080 check inter 10s rise 2 fall 3

    # Timeouts {{ portal_url | replace('-', '_') | replace('.', '_') }}_backend
    timeout connect 10s
    timeout server  1m
